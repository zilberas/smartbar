-if @menu_item_type.errors.any?
  #error_explanation
    %h2
      = pluralize(@menu_item_type.errors.count, "error")
      prohibited this account from being saved:
    %ul
      @menu_item_type.errors.full_messages.each do |msg|
      %li
        = msg

.field
  = f.label :name
  = f.text_field :name

.actions
  = f.submit "Update"